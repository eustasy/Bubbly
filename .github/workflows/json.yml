# eustasy/.Normal 4.0beta8
name: Normal (JSON)

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ["**.json"]
  pull_request:
    branches: [main]
    paths: ["**.json"]

permissions:
  contents: read

jobs:
  json:
    name: Format check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - uses: qltysh/qlty-action/install@a19242102d17e497f437d7466aa01b528537e899 # v2.2.0

      - name: Format check (prettier)
        run: git ls-files -- '*.json' | xargs -r qlty check --filter=prettier
