include directive/bubbly_gzip.conf;
include directive/h5bp_cache-file-descriptors.conf;
include location/h5bp_expires.conf;

# [OPTION] Brotli, alongside gzip rather than instead of it.
# [WARNING] Not enabled by default because without
# libnginx-mod-http-brotli-filter installed, `brotli on;` stops Nginx loading any
# config at all. Install it, then uncomment. See directive/bubbly_brotli.conf.
#include directive/bubbly_brotli.conf;
