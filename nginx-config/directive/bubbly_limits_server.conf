# 2000 concurrent connections for this site, whoever they come from — a blunt
# guard against one site consuming the whole machine.
# [WARNING] Unreachable unless worker_processes × worker_connections exceeds it.
# Debian and Ubuntu set worker_connections to 768, so this does nothing below
# three cores. Check with `nginx -T | grep worker_`.
limit_conn conPerServer 2000;
