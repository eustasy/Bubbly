- [Preserve config formatting](feedback_formatting.md) — don't flatten multi-line, aligned values when fixing bugs; use `set`-style accumulators or similar.
- [Version-option lists — descending, all supported, newest is default](feedback_version_lists.md) — verify support dates against php.net rather than trusting the file.
- [Version floor is Ubuntu 26.04 — nginx 1.25.1+ / OpenSSL 3.5+](project_nginx-version-floor.md) — verified per-platform versions, the two gates that set it, and why only distribution packages are used.
- [Per-vhost TLS: ciphers apply, protocols and curves do not](project_tls-per-vhost-findings.md) — measured on nginx 1.26 and 1.28; the default server decides, which is why `bubbly_default.conf` exists.
- [Open follow-ups](project_open-follow-ups.md) — small known issues left deliberately, and the missing CHANGELOG.
